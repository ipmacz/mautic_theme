<style type="text/css" scoped>
@import url('https://fonts.googleapis.com/css2?family=Exo+2:wght@400;500;600;700&family=Open+Sans:wght@400;600;700&display=swap');

:root {
  /* IPMA brand palette */
  --ipma-red:        #E30613;
  --ipma-red-hover:  #B8040F;
  --ipma-black:      #000000;
  --ipma-black-80:   #333333;
  --ipma-black-60:   #666666;
  --ipma-black-40:   #999999;
  --ipma-black-20:   #CCCCCC;
  --ipma-white:      #FFFFFF;
  --ipma-bg2:        #F5F5F5;
  --ipma-bg3:        #EAEAEA;
  /* Typography */
  --ipma-font-primary:   'Exo 2', 'Helvetica Neue', Arial, sans-serif;
  --ipma-font-secondary: 'Open Sans', 'Helvetica Neue', Arial, sans-serif;
  /* Radius */
  --ipma-radius:     8px;
  --ipma-radius-btn: 4px;
}

html {
  background-color: var(--ipma-bg2);
  box-sizing: border-box;
  font-size: 10px;
  font-family: var(--ipma-font-secondary);
  line-height: 1.55;
  -ms-text-size-adjust: 100%;
  -webkit-text-size-adjust: 100%;
}
*, *::before, *::after { box-sizing: inherit; }

.container-fluid.signup-form {
  max-width: 640px;
  margin: 0 auto;
  padding: 2.4rem;
  font-family: var(--ipma-font-secondary);
  color: var(--ipma-black-80);
}

/* Headings (field-group labels render inside h2; form header in h4) */
.signup-form h2,
.signup-form h4 {
  font-family: var(--ipma-font-primary);
  color: var(--ipma-black);
  line-height: 1.25;
  margin: 0 0 1.2rem;
}
.signup-form h2 { font-weight: 600; font-size: 1.8rem; }
.signup-form h4 { font-weight: 500; font-size: 2.2rem; margin-bottom: 2rem; }

/* Rows: stack label above field */
.mauticform-row {
  display: flex;
  flex-direction: column;
  margin-bottom: 1.8rem;
}

/* Static labels above input */
label.mauticform-label {
  order: -1;
  position: static;
  font-family: var(--ipma-font-secondary);
  font-weight: 600;
  font-style: normal;
  font-size: 1.5rem;
  line-height: 1.25;
  color: var(--ipma-black-80);
  margin-bottom: 0.6rem;
}

.mauticform-helpmessage {
  display: block;
  font-size: 1.3rem;
  color: var(--ipma-black-60);
  margin-bottom: 0.6rem;
}

/* Text inputs & textarea */
[type="text"],
[type="email"],
[type="date"],
[type="number"],
[type="tel"],
[type="url"],
[type="password"],
textarea {
  background-color: var(--ipma-white);
  border: 1px solid var(--ipma-black-20);
  padding: 1rem 1.5rem;
  border-radius: var(--ipma-radius);
  width: 100%;
  transition: border-color .2s ease, box-shadow .2s ease;
  font-family: var(--ipma-font-secondary);
  font-size: 1.5rem;
  font-style: normal;
  color: var(--ipma-black-80);
  line-height: 1.3;
}
textarea { min-height: 12rem; resize: vertical; }

[type="text"]:focus,
[type="email"]:focus,
[type="date"]:focus,
[type="number"]:focus,
[type="tel"]:focus,
[type="url"]:focus,
[type="password"]:focus,
textarea:focus,
select:focus {
  outline: none;
  border-color: var(--ipma-red);
  box-shadow: 0 0 0 3px rgba(227, 6, 19, .12);
}

::placeholder { color: var(--ipma-black-40); font-style: normal; }

/* Select */
.mauticform-row select,
select,
select.custom-select {
  display: block;
  width: 100%;
  padding: 1rem 3.5rem 1rem 1.5rem;
  font-family: var(--ipma-font-secondary);
  font-size: 1.5rem;
  font-style: normal;
  font-weight: 400;
  line-height: 1.3;
  color: var(--ipma-black-80);
  vertical-align: middle;
  background: var(--ipma-white) url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' width='4' height='5' viewBox='0 0 4 5'%3e%3cpath fill='%23666666' d='M2 0L0 2h4zm0 5L0 3h4z '/%3e%3c/svg%3e") no-repeat right 1.5rem center/8px 10px;
  border: 1px solid var(--ipma-black-20);
  border-radius: var(--ipma-radius);
  transition: border-color .2s ease, box-shadow .2s ease;
  -webkit-appearance: none;
  -moz-appearance: none;
  appearance: none;
}

@media only screen and (min-width: 480px) {
  .input-group [type="email"],
  .input-group [type="date"],
  .input-group [type="number"],
  .input-group [type="password"],
  .input-group [type="url"],
  .input-group [type="tel"],
  .input-group select {
    max-width: 34rem;
  }
}

/* Checkbox & radio groups */
/* Core Mautic markup: input + label are siblings inside the row */
.mauticform-checkboxgrp-row,
.mauticform-radiogrp-row {
  display: flex;
  flex-direction: row;
  align-items: center;
  gap: 0.8rem;
  margin-bottom: 0.8rem;
}

.mauticform-checkboxgrp-row input[type="checkbox"],
.mauticform-radiogrp-row input[type="radio"],
.mauticform-checkboxgrp-checkbox,
.mauticform-radiogrp-radio {
  flex: 0 0 auto;
  width: 1.8rem;
  height: 1.8rem;
  margin: 0;
  accent-color: var(--ipma-red);
  cursor: pointer;
}

.mauticform-checkboxgrp-row label,
.mauticform-radiogrp-row label,
label.mauticform-checkboxgrp-label,
label.mauticform-radiogrp-label {
  margin: 0;
  cursor: pointer;
  font-family: var(--ipma-font-secondary);
  font-size: 1.5rem;
  font-weight: 400;
  font-style: normal;
  color: var(--ipma-black-80);
  line-height: 1.3;
}

/* Theme markup: label wraps a hidden input + custom span marker */
.with-checkbox label {
  display: flex;
  flex-direction: row;
  align-items: center;
  gap: 0.8rem;
}
.with-checkbox span { margin-left: 0; }

.with-checkbox input[type="checkbox"],
.with-checkbox input[type="radio"] {
  position: absolute;
  opacity: 0;
  width: 0;
  height: 0;
}
.with-checkbox input[type="checkbox"] + span,
.with-checkbox input[type="radio"] + span {
  flex: 0 0 1.8rem;
  width: 1.8rem;
  height: 1.8rem;
  border: 1.5px solid var(--ipma-black-40);
  background: var(--ipma-white);
  display: inline-block;
  position: relative;
  transition: background .15s ease, border-color .15s ease;
}
.with-checkbox input[type="checkbox"] + span { border-radius: var(--ipma-radius-btn); }
.with-checkbox input[type="radio"] + span { border-radius: 50%; }

.with-checkbox input:checked + span {
  background: var(--ipma-red);
  border-color: var(--ipma-red);
}
.with-checkbox input[type="checkbox"]:checked + span::after {
  content: '';
  position: absolute;
  left: 0.55rem;
  top: 0.15rem;
  width: 0.5rem;
  height: 1rem;
  border: solid var(--ipma-white);
  border-width: 0 2px 2px 0;
  transform: rotate(45deg);
}
.with-checkbox input[type="radio"]:checked + span::after {
  content: '';
  position: absolute;
  left: 0.45rem;
  top: 0.45rem;
  width: 0.7rem;
  height: 0.7rem;
  border-radius: 50%;
  background: var(--ipma-white);
}
.with-checkbox input:focus + span {
  box-shadow: 0 0 0 3px rgba(227, 6, 19, .12);
}

/* Submit button */
.mauticform-button-wrapper { margin-top: 0.8rem; }
.button,
button[type="submit"] {
  font-family: var(--ipma-font-primary);
  font-weight: 600;
  font-size: 1.5rem;
  letter-spacing: 0.02em;
  width: auto;
  align-self: flex-start;
  padding: 1.2rem 2.8rem;
  border-radius: var(--ipma-radius-btn);
  border: 1.5px solid transparent;
  background: var(--ipma-red);
  color: var(--ipma-white);
  cursor: pointer;
  display: inline-flex;
  align-items: center;
  gap: 1rem;
  transition: background 160ms ease, border-color 160ms ease;
  white-space: nowrap;
}
.button:hover,
button[type="submit"]:hover { background: var(--ipma-red-hover); }
.button:focus,
button[type="submit"]:focus {
  outline: none;
  box-shadow: 0 0 0 3px rgba(227, 6, 19, .25);
}
.button svg { fill: currentColor; }

/* Validation errors */
.mauticform-has-error [type="date"],
.mauticform-has-error [type="text"],
.mauticform-has-error [type="email"],
.mauticform-has-error [type="url"],
.mauticform-has-error [type="password"],
.mauticform-has-error [type="number"],
.mauticform-has-error [type="tel"],
.mauticform-has-error [type="checkbox"] + span,
.mauticform-has-error [type="radio"] + span,
.mauticform-has-error select,
.mauticform-has-error textarea {
  border-color: var(--ipma-red);
}
.mauticform-has-error label.mauticform-label,
.mauticform-has-error label.mauticform-checkboxgrp-label,
.mauticform-has-error label.mauticform-radiogrp-label,
.mauticform-has-error h2 {
  color: var(--ipma-red);
}
.mauticform-errormsg,
.message-error {
  color: var(--ipma-red);
  font-size: 1.3rem;
  margin-top: 0.6rem;
}
</style>
